const translations = {
  zh: {
    brand: "林楷舜",
    navAbout: "關於",
    navProof: "事蹟",
    navResearch: "研究成果",
    navRadar: "能力雷達",
    navContact: "聯絡",
    heroEyebrow: "博士候選人",
    heroName: "林楷舜",
    heroRole: "醫療管理與數據策略新秀",
    heroIntro: "把醫療現場的複雜訊號，整理成能被管理者、臨床團隊與病人一起理解的行動方案。",
    heroCta: "建立聯繫",
    heroSecond: "查看研究成果",
    factGpa: "GPA / 4.3",
    factProjects: "專案成果",
    factCerts: "專業證照",
    panelFocus: "Focus",
    panelFocusValue: "醫療營運分析",
    panelNow: "Now",
    panelNowValue: "AI 病患流程模擬",
    tickerOne: "Healthcare Operations",
    tickerTwo: "Patient Journey",
    tickerThree: "Evidence-Based Management",
    aboutEyebrow: "About",
    aboutTitle: "關於我",
    aboutBody: "林楷舜目前為博士候選人，主修醫療管理、健康資料分析與服務設計。擅長把流程圖、問卷資料、儀表板與訪談洞察整合成可執行的改善提案。",
    eduLabel: "學歷",
    eduValue: "博士候選人｜健康產業管理與醫療資料分析",
    expLabel: "經歷",
    expValue: "醫院流程改善實習生、校園健康資料專案 PM",
    certLabel: "證照",
    certValue: "Google Data Analytics、初階專案管理、病人安全教育訓練",
    mottoLabel: "一句話",
    mottoValue: "讓資料有溫度，讓管理有方向。",
    proofEyebrow: "Milestones",
    proofTitle: "事蹟",
    achOneTitle: "校園健康黑客松冠軍",
    achOneBody: "用 24 小時產出睡眠風險分群原型，整合問卷、穿戴資料與行為建議。",
    achTwoTitle: "國際交流簡報代表",
    achTwoBody: "以英文發表臺灣智慧醫療服務觀察，獲選為管理學院跨校交流學生代表。",
    achThreeTitle: "研究方法工作坊講者",
    achThreeBody: "設計醫療管理資料視覺化短講，帶領 60 位同學完成互動式儀表板練習。",
    achFourTitle: "病人安全改善提案入選",
    achFourBody: "以跨科別溝通流程為題，提出交班資訊缺口偵測與追蹤回饋機制。",
    researchEyebrow: "Research Outputs",
    researchTitle: "研究成果",
    workOneTitle: "急診等候時間視覺化",
    workOneBody: "以假想醫院資料建立 Power BI 儀表板，提出分流尖峰預警與護理人力調度模型。",
    workTwoTitle: "慢病照護 App 研究",
    workTwoBody: "完成 48 份使用者訪談與服務藍圖，設計三階段回診提醒與家屬共照機制。",
    workThreeTitle: "醫院營運指標白皮書",
    workThreeBody: "整理門診量能、床位週轉、病人滿意度與候診體驗，做成可追蹤的 KPI 架構。",
    workFourTitle: "健康資料倫理摘要",
    workFourBody: "撰寫 AI 輔助決策、資料去識別化與病人授權流程的虛擬研究摘要。",
    dashEyebrow: "Live Profile",
    dashTitle: "動態能力雷達",
    dashBody: "虛擬人物資料以動態方式呈現，適合之後改成真實履歷、研究成果或作品集數據。",
    skillOne: "醫療流程分析",
    skillTwo: "資料視覺化",
    skillThree: "簡報與溝通",
    skillFour: "服務設計",
    contactEyebrow: "Contact",
    contactTitle: "聯絡我",
    contactBody: "歡迎交換醫療管理、健康科技、資料視覺化與學生專案合作機會。",
    footer: "© 2026 林楷舜. Fictional profile demo."
  },
  en: {
    brand: "Kai-Shun Lin",
    navAbout: "About",
    navProof: "Achievements",
    navResearch: "Research",
    navRadar: "Radar",
    navContact: "Contact",
    heroEyebrow: "PhD Candidate",
    heroName: "Kai-Shun Lin",
    heroRole: "Healthcare Management & Data Strategy Talent",
    heroIntro: "I turn complex signals from healthcare settings into action plans that leaders, clinical teams, and patients can understand together.",
    heroCta: "Connect",
    heroSecond: "View Research",
    factGpa: "GPA / 4.3",
    factProjects: "Projects",
    factCerts: "Certificates",
    panelFocus: "Focus",
    panelFocusValue: "Healthcare Operations Analytics",
    panelNow: "Now",
    panelNowValue: "AI Patient Flow Simulation",
    tickerOne: "Healthcare Operations",
    tickerTwo: "Patient Journey",
    tickerThree: "Evidence-Based Management",
    aboutEyebrow: "About",
    aboutTitle: "About Me",
    aboutBody: "Kai-Shun Lin is a PhD candidate focusing on healthcare management, health data analytics, and service design. He connects process maps, survey data, dashboards, and interview insights into practical improvement proposals.",
    eduLabel: "Education",
    eduValue: "PhD Candidate | Healthcare Management & Health Data Analytics",
    expLabel: "Experience",
    expValue: "Hospital process improvement intern, campus health data project PM",
    certLabel: "Certificates",
    certValue: "Google Data Analytics, Junior Project Management, Patient Safety Training",
    mottoLabel: "Motto",
    mottoValue: "Give data warmth. Give management direction.",
    proofEyebrow: "Milestones",
    proofTitle: "Achievements",
    achOneTitle: "Campus Health Hackathon Winner",
    achOneBody: "Created a sleep-risk segmentation prototype in 24 hours using surveys, wearable data, and behavior guidance.",
    achTwoTitle: "International Presentation Delegate",
    achTwoBody: "Presented observations on Taiwan smart healthcare services in English as a cross-school exchange representative.",
    achThreeTitle: "Research Methods Workshop Speaker",
    achThreeBody: "Designed a healthcare management data visualization session and guided 60 classmates through dashboard exercises.",
    achFourTitle: "Patient Safety Proposal Finalist",
    achFourBody: "Proposed a handoff-gap detection and feedback workflow for cross-department communication.",
    researchEyebrow: "Research Outputs",
    researchTitle: "Research Results",
    workOneTitle: "ER Wait-Time Visualization",
    workOneBody: "Built a Power BI dashboard from fictional hospital data and proposed peak-alert triage and nurse staffing models.",
    workTwoTitle: "Chronic Care App Research",
    workTwoBody: "Completed 48 user interviews and a service blueprint for follow-up reminders and family co-care workflows.",
    workThreeTitle: "Hospital Operations KPI White Paper",
    workThreeBody: "Mapped outpatient capacity, bed turnover, patient satisfaction, and waiting experience into a trackable KPI framework.",
    workFourTitle: "Health Data Ethics Abstract",
    workFourBody: "Drafted a fictional research abstract on AI decision support, de-identification, and patient authorization workflows.",
    dashEyebrow: "Live Profile",
    dashTitle: "Dynamic Capability Radar",
    dashBody: "This fictional profile data is presented dynamically and can later be replaced with real resume, research, or portfolio metrics.",
    skillOne: "Healthcare Process Analysis",
    skillTwo: "Data Visualization",
    skillThree: "Presentation & Communication",
    skillFour: "Service Design",
    contactEyebrow: "Contact",
    contactTitle: "Contact Me",
    contactBody: "Open to conversations about healthcare management, health technology, data visualization, and student project collaboration.",
    footer: "© 2026 Kai-Shun Lin. Fictional profile demo."
  }
};

const state = {
  lang: localStorage.getItem("profile-lang") || "zh",
  theme: localStorage.getItem("profile-theme") || "light"
};

const root = document.documentElement;
const langToggle = document.querySelector("#langToggle");
const themeToggle = document.querySelector("#themeToggle");

function setTheme(theme) {
  state.theme = theme;
  root.dataset.theme = theme;
  localStorage.setItem("profile-theme", theme);
  themeToggle.setAttribute("aria-label", theme === "dark" ? "Switch to light theme" : "Switch to dark theme");
}

function setLanguage(lang) {
  state.lang = lang;
  document.documentElement.lang = lang === "zh" ? "zh-Hant" : "en";
  document.querySelectorAll("[data-i18n]").forEach((node) => {
    const key = node.dataset.i18n;
    node.textContent = translations[lang][key] || node.textContent;
  });
  langToggle.textContent = lang === "zh" ? "EN" : "中";
  localStorage.setItem("profile-lang", lang);

  if (window.gsap) {
    gsap.fromTo("[data-i18n]", { y: 8, opacity: 0.68 }, {
      y: 0,
      opacity: 1,
      duration: 0.36,
      ease: "power2.out",
      stagger: 0.006
    });
  }
}

langToggle.addEventListener("click", () => setLanguage(state.lang === "zh" ? "en" : "zh"));
themeToggle.addEventListener("click", () => setTheme(state.theme === "dark" ? "light" : "dark"));

setTheme(state.theme);
setLanguage(state.lang);

function initGsap() {
  if (!window.gsap) {
    document.querySelectorAll("[data-reveal]").forEach((el) => {
      el.style.opacity = 1;
      el.style.transform = "none";
    });
    document.querySelectorAll(".bar i").forEach((bar) => {
      bar.style.width = bar.style.getPropertyValue("--value");
    });
    return;
  }

  gsap.registerPlugin(ScrollTrigger);

  gsap.to("[data-reveal]", {
    opacity: 1,
    y: 0,
    duration: 0.9,
    ease: "power3.out",
    stagger: 0.08,
    scrollTrigger: {
      trigger: "body",
      start: "top 86%"
    }
  });

  document.querySelectorAll("[data-reveal]").forEach((el) => {
    gsap.fromTo(el, { opacity: 0, y: 44 }, {
      opacity: 1,
      y: 0,
      duration: 0.85,
      ease: "power3.out",
      scrollTrigger: {
        trigger: el,
        start: "top 86%",
        toggleActions: "play none none reverse"
      }
    });
  });

  gsap.from(".hero-title span", {
    yPercent: 115,
    rotateX: -70,
    opacity: 0,
    duration: 1.1,
    ease: "expo.out",
    stagger: 0.12
  });

  gsap.to(".hero-visual img", {
    y: -18,
    rotate: 1.5,
    duration: 3.2,
    ease: "sine.inOut",
    repeat: -1,
    yoyo: true
  });

  gsap.to(".floating-panel", {
    y: -16,
    duration: 2.4,
    ease: "sine.inOut",
    repeat: -1,
    yoyo: true,
    stagger: 0.35
  });

  gsap.to(".bar i", {
    width: (_, el) => el.style.getPropertyValue("--value"),
    duration: 1.3,
    ease: "expo.out",
    stagger: 0.12,
    scrollTrigger: {
      trigger: ".skill-board",
      start: "top 78%"
    }
  });

  document.querySelectorAll(".counter").forEach((counter) => {
    const target = Number(counter.dataset.target);
    const decimals = String(target).includes(".") ? 2 : 0;
    gsap.to(counter, {
      textContent: target,
      duration: 1.4,
      ease: "power2.out",
      snap: { textContent: decimals ? 0.01 : 1 },
      onUpdate: () => {
        counter.textContent = Number(counter.textContent).toFixed(decimals);
      },
      scrollTrigger: {
        trigger: counter,
        start: "top 86%",
        once: true
      }
    });
  });
}

function initTiltCards() {
  const cards = document.querySelectorAll(".tilt-card");

  cards.forEach((card) => {
    card.addEventListener("pointermove", (event) => {
      const rect = card.getBoundingClientRect();
      const x = (event.clientX - rect.left) / rect.width - 0.5;
      const y = (event.clientY - rect.top) / rect.height - 0.5;
      card.style.transform = `rotateX(${y * -8}deg) rotateY(${x * 10}deg) translateY(-4px)`;
    });

    card.addEventListener("pointerleave", () => {
      card.style.transform = "";
    });
  });
}

function initMagneticButtons() {
  document.querySelectorAll(".magnetic").forEach((button) => {
    button.addEventListener("pointermove", (event) => {
      const rect = button.getBoundingClientRect();
      const x = event.clientX - rect.left - rect.width / 2;
      const y = event.clientY - rect.top - rect.height / 2;
      button.style.transform = `translate(${x * 0.18}px, ${y * 0.24}px)`;
    });

    button.addEventListener("pointerleave", () => {
      button.style.transform = "";
    });
  });
}

function initParticles() {
  const canvas = document.querySelector("#particleCanvas");
  const ctx = canvas.getContext("2d");
  const particles = [];
  const pointer = { x: 0, y: 0, active: false };
  let width = 0;
  let height = 0;
  let rafId = 0;

  function resize() {
    const dpr = Math.min(window.devicePixelRatio || 1, 2);
    width = window.innerWidth;
    height = window.innerHeight;
    canvas.width = Math.floor(width * dpr);
    canvas.height = Math.floor(height * dpr);
    canvas.style.width = `${width}px`;
    canvas.style.height = `${height}px`;
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);

    particles.length = 0;
    const count = Math.min(92, Math.max(42, Math.floor(width / 18)));
    for (let i = 0; i < count; i += 1) {
      particles.push({
        x: Math.random() * width,
        y: Math.random() * height,
        vx: (Math.random() - 0.5) * 0.55,
        vy: (Math.random() - 0.5) * 0.55,
        r: Math.random() * 2.2 + 0.8
      });
    }
  }

  function draw() {
    ctx.clearRect(0, 0, width, height);
    const isDark = state.theme === "dark";
    const dot = isDark ? "rgba(0, 224, 255, 0.82)" : "rgba(0, 107, 234, 0.62)";
    const line = isDark ? "rgba(125, 255, 223, 0.13)" : "rgba(0, 107, 234, 0.12)";

    particles.forEach((p, index) => {
      p.x += p.vx;
      p.y += p.vy;

      if (p.x < -20) p.x = width + 20;
      if (p.x > width + 20) p.x = -20;
      if (p.y < -20) p.y = height + 20;
      if (p.y > height + 20) p.y = -20;

      if (pointer.active) {
        const dx = pointer.x - p.x;
        const dy = pointer.y - p.y;
        const dist = Math.hypot(dx, dy);
        if (dist < 150) {
          p.x -= dx * 0.004;
          p.y -= dy * 0.004;
        }
      }

      ctx.beginPath();
      ctx.fillStyle = dot;
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fill();

      for (let j = index + 1; j < particles.length; j += 1) {
        const other = particles[j];
        const dx = p.x - other.x;
        const dy = p.y - other.y;
        const dist = Math.hypot(dx, dy);
        if (dist < 118) {
          ctx.strokeStyle = line;
          ctx.lineWidth = 1 - dist / 118;
          ctx.beginPath();
          ctx.moveTo(p.x, p.y);
          ctx.lineTo(other.x, other.y);
          ctx.stroke();
        }
      }
    });

    rafId = requestAnimationFrame(draw);
  }

  window.addEventListener("resize", resize);
  window.addEventListener("pointermove", (event) => {
    pointer.x = event.clientX;
    pointer.y = event.clientY;
    pointer.active = true;
  });
  window.addEventListener("pointerleave", () => {
    pointer.active = false;
  });

  resize();
  draw();

  document.addEventListener("visibilitychange", () => {
    if (document.hidden) {
      cancelAnimationFrame(rafId);
    } else {
      draw();
    }
  });
}

window.addEventListener("load", () => {
  initGsap();
  initTiltCards();
  initMagneticButtons();
  initParticles();
});
